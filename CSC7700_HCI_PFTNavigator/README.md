ALL pngs in `assets` folder are the same size, same coordinate, please do not modify.

There are two example json in `data` folder. You can also use `annotate.html` to create new data entries. This is the most important work for now.
